"""

  Largest product in a series
  
  Complete the greatestProduct method so that 
  it'll find the greatest product of five consecutive 
  digits in the given string of digits.
    
"""

def greatest_product(n):
    r = [int(x) for x in str(int(n))]
    maxnum = 0
    for x in range(len(r)):
        if x == (len(res) - 4):
            break 
        num = r[x] * r[x + 1] * r[x + 2] * r[x + 3] * r[x + 4]
        if num > maxnum:
            maxnum = num
            
    return maxnum
